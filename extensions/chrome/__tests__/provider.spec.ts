/**
 * Basic provider wiring test — adjust import paths to your real provider once implemented.
 */
describe('extension provider wiring', () => {
  it('bootstraps without crashing', () => {
    expect(true).toBe(true);
  });
});